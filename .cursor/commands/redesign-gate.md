# redesign-gate

Apply the **Editorial Commerce Ledger** visual system from `.cursor/rules/frontend-design.mdc` and `docs/ILLUSTRATIONS.md`.

Forbidden: MasterFabric sample skins (navy `#0b0f17`, blue `#4f8cff`), purple AI themes, cream+terracotta templates.

Do: Fraunces + DM Sans, teal accent `#0F766E`, cool stone canvas, responsive Gate Mock|Manual, clear PASS/REVIEW/REJECT.

Touch `globals.css`, Auth, AppShell, GateView — keep components lean.
